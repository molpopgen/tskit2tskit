[package]
name = "tskit2tskit"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2.183"
pyo3 = "0.27.0"
tskit = { version = "0.15.0-alpha.4", features = ["derive", "bindings", "unsafe_init"] }

[dev-dependencies]
pyo3 = { version = "0.27.0", features = ["auto-initialize"] }

